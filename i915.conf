#options i915 modeset=1
# Enable kernel mode setting for Intel graphics

#options i915 enable_fbc=1
# Enable Frame Buffer Compression

options i915 enable_guc=3
# Unified option for enabling GuC/HuC firmware. (0: disable, 1: load GuC only, 2: GuC submission, 3: load both GuC and HuC firmware

options i915 enable_psr=1
# Enable Panel Self Refresh

options i915 disable_power_well=0

# save it under /etc/modprobe.d/
